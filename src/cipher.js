/* no se utiliza this en cipher*/
/*const cipher =(string.toUpperCase){
  cipher.encode = "";
  cipher.decode = "";
  window.cipher.encode = "";
  window.cipher.decode = "";
  
function createCipherWithOffset():  
cipher.createCipherWithOffset()
}
let abc = "abcdefghijklmnñopqrstuvwxyz" {

}
  

window.cipher =  {
  encode: (: )=> () {
    /* Acá va tu código (la flecha reemplaza a function)*/
   /* cipher.encode()
  }
  decode: () => {
    /* Acá va tu código */
  /*  cipher.decode()
  } 
}
window.cipher.decode(;) */

cipherencode:(abc, offset) => { //dando nombre a la funcion
  var textoacifrar1 = abc.toUpperCase();
  var textoacifrar2 = "";
  console.log(textoacifrar1)
  for (i = 0; i < textoacifrar1.length; i++) { //busca codigo ASCII y guarda la letra en textoacifrar1

    var offset = textoacifrar1.charCodeAt(i);
    if (offset == 35) 
  }
  textoacifrar2 = textoacifrar2 + string.charCodeAt(offset); //guarda el espacio
 
}